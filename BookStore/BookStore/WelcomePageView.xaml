<Page
    x:Class="BookStore.WelcomePageView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BookStore"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Text="Welcome to Book Store Manager"
                   Grid.Row="0"
                   FontSize="50"
                   VerticalAlignment="Top" 
                   HorizontalAlignment="Center"
                   Margin="50"/>
        <Image Source="/Assets/scary2.png" Width="150" Height="150" 
               Grid.Row="1" Visibility="Visible"
              />

        <TextBlock Text="by Yuval Kfir"
                   Grid.Row="2"
                   VerticalAlignment="Bottom"
                   HorizontalAlignment="Center"
                   Margin="10" FontSize="10"/>
    </Grid>
</Page>
